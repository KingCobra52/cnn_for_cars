# carvision

Fine-grained car classification on Stanford Cars (196 classes), built under a hard
CPU-only constraint.

> **Status: under construction.** This README is rewritten in Phase 6 with the real
> results table, figures and demo link. See `docs/` as it fills in.

The previous contents of this repository — a single Colab notebook classifying CIFAR-10
into `background` / `car` / `truck` — are preserved in
[`notebooks/legacy/`](notebooks/legacy/README.md) along with a writeup of the bugs it
shipped with, which is what motivated this rebuild.
