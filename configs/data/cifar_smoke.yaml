# The legacy 3-class CIFAR task, for fast end-to-end smoke runs.
#
# It is built locally by carvision.data.cifar_smoke rather than pulled from the Hub, so
# the mirror fields are unused here and left at their defaults.
name: cifar_smoke
num_classes: 3
val_fraction: 0.15
split_seed: 20260918
